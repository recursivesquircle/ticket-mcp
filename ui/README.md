# Ticket UI

React + Vite UI for browsing and updating repo tickets via MCP.

## Usage

```
npm install
npm run dev
```

By default it targets `http://127.0.0.1:3334/mcp`.

Override with:

```
VITE_TICKET_MCP_URL=http://127.0.0.1:3334/mcp
```
